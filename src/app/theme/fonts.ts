import { Red_Hat_Display, Parisienne, Carattere } from "next/font/google";

export const redHatDisplay = Red_Hat_Display({
  subsets: ["latin"],
});

export const parisienne = Parisienne({
  subsets: ["latin"],
  weight: "400",
});

export const carattere = Carattere({
  subsets: ["latin"],
  weight: "400",
});
